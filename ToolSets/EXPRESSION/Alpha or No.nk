set cut_paste_input [stack 0]
version 6.3 v4
push $cut_paste_input
NoOp {
 name Alpha_or_No
 label "\[python \"len(\\\[n for n in nuke.channels(nuke.thisNode().input(0)) if n.find(\\\".a\\\") != -1])>0\"]"
 selected true
 xpos -1252
 ypos -33
}
