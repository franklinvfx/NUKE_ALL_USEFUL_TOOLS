#-----------------------------------------------------------------------------------------------------------------
# IMPORT NUKE PIPE
#-----------------------------------------------------------------------------------------------------------------

import nuke, sys, platform

if platform.system() == "Darwin":
	pipe_path = "/Network/phatt/MMP/NUKE/NUKE_PIPE_DEV/"
	
elif platform.system() == "Windows":
	pipe_path = "Y:\\MMP\\NUKE\\NUKE_PIPE_DEV\\"

nuke.pluginAddPath(pipe_path);

import MM_Config
# import Home_Config
# import Other_Config


#-----------------------------------------------------------------------------------------------------------------
# DEBUG OPTIONS
#-----------------------------------------------------------------------------------------------------------------
# import callbacksTrace                 # show all callbacks